name: Merge Pull Request
on: pull_request
env:
  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
jobs:
  Start:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: actions/checkout@v3
      - name: merge
        run: |
          echo ${{ github.head_ref }}
          gh pr merge ${{ github.head_ref }} --admin -m -d